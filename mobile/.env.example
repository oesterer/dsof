# Local builds: copy this file to .env.local. The target plist must remain ignored.
# EAS builds: upload the plist as a secret file variable with this same name.
GOOGLE_SERVICES_INFO_PLIST=./secrets/GoogleService-Info.plist

# Optional: show the iPad sensor diagnostics overlay.
EXPO_PUBLIC_SHOW_SENSOR_DEBUG=false
